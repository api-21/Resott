<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Risotto" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary_500</item>
        <item name="colorPrimaryVariant">@color/p_dark_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/white</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.SplashTheme" parent="Theme.AppCompat.DayNight.NoActionBar">

        <item name="android:windowBackground">@drawable/splash_background</item>

    </style>

    // Logo Text

    <style name="Theme.Logo" parent="Widget.AppCompat.TextView">
        <item name="fontFamily">@font/tajawal_black</item>
        <item name="android:text">@string/app_name</item>
        <item name="android:textAppearance">@style/TextAppearance.MaterialComponents.Headline2
        </item>
        <item name="android:textColor">@color/primary_500</item>

    </style>

    //Fonts Styles
    <style name="Theme.TextStyleBold" parent="Widget.AppCompat.TextView">
        <item name="fontFamily">@font/tajawal_black</item>
        <item name="android:textSize">16dp</item>
    </style>

    <style name="Theme.TextStyleMedium" parent="Widget.AppCompat.TextView">
        <item name="fontFamily">@font/tajawal_medium</item>
        <item name="android:textSize">16dp</item>
    </style>

    <style name="Theme.TextStyleRegular" parent="Widget.AppCompat.TextView">
        <item name="fontFamily">@font/tajawal_regular</item>
        <item name="android:textSize">16dp</item>
    </style>
</resources>